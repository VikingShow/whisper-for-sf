[pytest]
# pytest 配置文件

# 测试路径
testpaths = tests

# 输出选项
addopts = 
    -v
    --tb=short
    --strict-markers

# Python 路径
pythonpath = .

# 标记
markers =
    slow: 标记慢速测试
    integration: 标记集成测试

# 忽略的警告
filterwarnings =
    ignore::DeprecationWarning


